{
  "name": "unload-tabs-firefox-addon",
  "version": "1.1.0",
  "private": true,
  "description": "Firefox addon to unload inactive tabs and free memory",
  "scripts": {
    "test": "node --test test/tab-utils.test.js",
    "lint": "web-ext lint",
    "build": "web-ext build",
    "start": "mkdir -p ${TMPDIR:-$HOME/tmp} && TMPDIR=${TMPDIR:-$HOME/tmp} web-ext run"
  },
  "devDependencies": {
    "web-ext": "^8.0.0"
  }
}
